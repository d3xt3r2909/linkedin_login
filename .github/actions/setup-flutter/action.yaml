name: Setup Flutter
description: "Setup Flutter SDK"

runs:
  using: composite
  steps:
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: "3.38.4"
        channel: "stable"
        cache: true
